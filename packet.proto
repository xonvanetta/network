syntax = "proto3";
import "any.proto";

package network;

message Packet {
    uint64 type = 1;

    //To define the a unique connection
    string UUID = 2;

    //Any the inside packet
    google.protobuf.Any message = 3;
}