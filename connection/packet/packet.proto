syntax = "proto3";
import "any.proto";
option go_package = "github.com/xonvanetta/network/connection/packet";

package packet;

message Packet {
    //Type
    uint64 type = 1;

    //Any the inside packet
    google.protobuf.Any message = 2;
}